## 0. CURUTULENLER (atlanan)

- [Doğruluk#1] Sürükleme mesafesi çift uygulanıyor → mevcut kodda tek-entegrasyon-noktası (SosyalObezite.tsx:152-176) ile zaten düzeltilmiş; bekleyenDy/momentum dalları ayrık, sayım offset-delta'dan.
- [Doğruluk#5 / React#40] route.ts'ten `export trGunu` build'i düşürür → trGunu lib/game/gun.ts'e taşınmış, submit/route.ts:7 import ediyor; route yalnız POST+runtime+dynamic export ediyor.
- [Erişilebilirlik#1] SR sinyali hiç yok → sr-only aria-live="assertive" anons bölgesi (SosyalObezite.tsx:419-426) + spawn/yakala/kaç/yanlış-dokunma anonsları (:184,:191,:260,:264) eklenmiş.
- [Erişilebilirlik#12] aria-live=polite skor spam'i → skor `<p>`'sinde aria-live yok (:364-366, bilinçli yorumla); tek anons bölgesi kullanılıyor.
- [React#1 / Spec#1] /sosyal-obezite/s/[runId] rotası yok, viral link 404 → app/sosyal-obezite/s/[runId]/page.tsx + opengraph-image.tsx diskte; UUID-regex (:35), redis.get, generateMetadata, graceful degrade mevcut.
- [React#5 / Doğruluk#6] setPointerCapture yokluğu = sonsuz kayma → mevcut mimaride surukleniyor dalı yalnız bekleyenDy tüketir; move gelmeyince akış DURUR, iddia edilen sonuç üretilemez (kalan hover-tuhaflığı bkz. §3).
- [Performans#30] Route-group yok, global süs katmanları oyunla koşuyor → SiteKatmanlari sadeMi('/sosyal-obezite') ile LiveTicker/CursorBean/ScrollManager/CookieConsent render edilmiyor; grain-overlay `html[data-oyun='1']` ile kapalı (globals.css:177). Kalan küçük parça (PostHog autocapture + CompassTint) §2'de.

## 1. CANLIYA CIKMAYI BLOKLAYANLAR (sırayla)

1. **app/api/game/start/route.ts + app/api/game/submit/route.ts + lib/game/nickname.ts** | Skor tablosu kimlik modeli spec §5.3'e dönecek: start'ta httpOnly cookie ile anonId üret; ZADD member'ı `ad.temiz` (submit:130-131) yerine anonId yap; takma adı `nick:{anonId}` hash'inde tut; ad sahipliği için `SETNX nick:{iskelet}→anonId` rezervasyonu (anahtar DAİMA iskelet, ham form değil); iskelette SINIF_HARITASI-dışı harfi (Yunanca ο vb.) SİLME (nickname.ts:73) yerine REDDET — silme iskeleti kısaltıp taklit kapısı bırakıyor | Neden: herkes herkesin adını `takmaAd:'Ayşe'` ile ezebiliyor, GT tek yönlü olduğundan 1605'e sabitlenen ad kalıcı işgal; 'Ayşe'/'ayşe'/'AYŞE' N ayrı satır; run:{runId} sahte kimlikle OG kartı üretir. Tabloya gerçek veri dolduktan sonra member migration'ı canlı tabloyu kırar — bu yüzden 1 numara.
2. **app/api/game/start/route.ts:33 + submit/route.ts:58 civarı** | Origin denetimi: "Origin header'ı VARSA ve `https://clubbeans.com` (+ *.vercel.app preview) ile eşleşmiyorsa 403" — Origin yoksa geçir (eski WebView'lar boş gönderir) | Neden: madde 1'in anonId cookie'si eklendiği an bugünkü content-type'sız simple-request açığı gerçek CSRF yazma vektörüne yükselir; cookie'den ÖNCE kapatılmalı.
3. **components/game/SosyalObezite.tsx:142-150 + OyunKabuk.tsx:69 + start/route.ts:59-63** | (a) visibilitychange: hidden anını kaydet, dönüşte `s.baslangic`'i gizli süre kadar kaydır (spawn takvimi gecen'e bağlı, otomatik düzelir); (b) /api/game/start çağrısını mount'tan '60 saniye' butonuna taşı (itiraf SosyalObezite içinde ve bileşen seed prop'uyla mount oluyor — itirafı OyunKabuk'a çıkar veya seed'siz ara durum ekle) VEYA asgari: sess TTL'yi 180→600 sn yap; (c) 409 metnini 'zaten kaydedildi' vs 'süre doldu' ayır | Neden: mobil birincil kitlede bildirime geçen oyuncu dönüşte 'tüm gerçekleri kaçırdın' ekranı görür (bitir() kalan spawn'ları kacir yazar, :115-125); itirafta 2 dk oyalanan dürüst oyuncunun submit'i getdel-null→409 ile kaybolur. İkisi aynı zaman bütçesi — istemci pause'u TTL hizalaması olmadan yine 409 üretir, birlikte yapılmalı.
4. **components/game/SosyalObezite.tsx:179-185 + lib/game/engine.ts:72-88** | Spawn while gövdesinde yeni atamadan önce `if (s.aktifGercek) { s.kacan++; s.olaylar.push('kacir'); }`; spawnTakvimi'nde ardışık spawn arası ≥2000ms taban (ilkTur override :80-82 sonrası da) | Neden: ilk turda ~%27 oranla (20K örnek, min gap 12ms) ikinci spawn ilkinin 1800ms penceresi kapanmadan gelir; ezilen gerçek ne yakalanan ne kacan sayılır → tur sonu '6/11' gösterir, sessiz veri kaybı.
5. **app/api/game/submit/route.ts:84-138** | getdel sonrası bloğu try/catch'e al + captureError; `set run:` + `zadd×2` + `expire` tek pipeline; hata halinde '500: kayıt tamamlanamadı, tekrar dene' (409'dan ayrık mesaj; sess restore denemesi opsiyonel — bkz §5); aynı pipeline değişikliğinde `gun = trGunu(new Date(oturum.baslangic))` (spec §5.4: bucket start anından) ve oturuma `Number.isFinite(gecen)` + Zod parse guard'ı (bozuk oturum → 422, 409 değil) | Neden: 5 ayrı await'ten biri viral yükte throw ederse sess tüketilmiş, zadd yapılmamış → retry 409 'zaten kaydedildi' yalanı, dürüst skor kalıcı yutulur; kısmi yazımda OG kartı var/tabloda skor yok; expire tek başına düşerse lb:day kalıcılaşır.
6. **app/api/game/submit/route.ts:59-61** | 429 yanıtına HILE_REDDI (feed.ts:149) yerine nötr metin ('Şu an yoğunluk var, birazdan tekrar dene' — start:40-43'teki nazik desenle tutarlı) + Retry-After header'ı; istemcide skoruGonder 429'da tek otomatik retry | Neden: TR mobil operatörleri CGNAT — viral tepede aynı IP'den 16. submit'i yapan dürüst oyuncu 'Bu skor akla yatmadı' ile hileci ilan edilir; ekran görüntüsü krizi riski.
7. **components/game/SosyalObezite.tsx:207-216** | Kart recycle anahtarına kademe ekle: `el.dataset.idx !== \`${index}:${kademe}\`` | Neden: hiç kaydırmayan oyuncu 60 sn boyunca mount anındaki 'anlamli' kartları görür — 12/25/38 sn eşikleri görünür kartlara yansımaz, oyunun tez taşıyıcısı 'boşalma yayı' hiç yaşanmaz. Yan etki (eşik frame'inde 12 textContent yazımı) ucuz; göz önünde metin değişimi bilinçli-sapma notuyla kabul.
8. **lib/game/nickname.ts:24** | 'pic ' kökünü GOMULU'dan çıkar, TAM_ESLESME token setine 'pic' ekle ('picler' GOMULU'da kalır) | Neden: canlı testle kanıtlı — 'Picasso', 'Epic35', 'Tropical' bugün 422 alıyor; iskelet boşluğu sildiği için kelime-sınırı kökü çıplak substring'e dönüşmüş.

## 2. CIKABILIR AMA HEMEN DUZELTILMELI

1. **SosyalObezite.tsx:233-245** | setHud'a shallow-compare (değerler değişmediyse çağırma — commit ~%80 düşer) + HUD ve anons state'ini ayrı child bileşenlere izole et; hedef durum: HUD'u rAF içinde ref'li textContent ile sürmek (spec §4 'commit=0' kabulünü harfiyen karşılar) | ~240 commit/tur + anons commit'leri spec §4 Profiler kabul testini otomatik FAIL ettiriyor; pratik jank hafif ama spec-uyum ihlali kesin.
2. **SosyalObezite.tsx:311-313 + :291-301 + :30-31** | (a) Klavye: basılı-tutmada sürtünmeyi bypass eden sabit hız (dokunmatik 8 kart/sn tavanına eşit); (b) mobile ≥44×44 'kaydır' + 'yakala' düğmeleri; (c) tap eşiği 260ms→500ms, 10px→16-18px (24px DEĞİL — kısa kaydırma niyetini tap'a çevirip haksız -20 üretir) | Klavye skor tavanı ~1305 vs 1605 yapısal dezavantaj; mobilde switch-access alternatifi sıfır; tremorlu kullanıcının dokunuşu sessizce yutuluyor.
3. **SosyalObezite.tsx:340-344, :363, :370, :437-438 + OyunKabuk.tsx:189** | Yalnız oyun yüzeylerinde text-ghost → daha açık sınıf (örn. text-white/60); global ghost token'ına DOKUNMA (site geneli regresyon) | #737373 kontrastı 3.98–4.30:1, hepsi 4.5:1 altında ve 10-12px; klavye alternatifini duyuran tek metin okunmuyor.
4. **SosyalObezite.tsx:162-166, :291-301, :311-313** | reduced=true iken onUp'ta `s.hiz=0` + klavye dalına sabit-adım (kart kart) ilerleme | Fırlatma sonrası ~1.7 sn (101 frame) tam-ekran atalet kayması; spec §10 'RM'de otomatik akış hızı 0' şartı hiç kodlanmamış.
5. **SosyalObezite.tsx:333-339, :376-387 + OyunKabuk tur-sonu** | setDurum('oynaniyor') sonrası `kokRef.current?.focus()` + aria-label'a klavye talimatı; tur-sonu ekranına tabIndex={-1} h1 + mount'ta focus | Odak body'ye düşüyor, SR kullanıcısı oyunun başladığını/bittiğini bilmiyor (WCAG 2.4.3).
6. **start/route.ts:31-32 + submit/route.ts:51-52** | `x-real-ip`'i öncele, XFF'i hiç okuma; 'bilinmiyor' fallback'ini ayrı sıkı limite bağla | Spec §5.3'ün isimle yasakladığı soldan-XFF deseni aynen kopyalanmış; Vercel-önü proxy/preview senaryosunda ratelimit tek header'la düşer (prod'da bugün repro edilemiyor — o yüzden blocker değil).
7. **OyunKabuk.tsx:50-51, :69** | turNo artırımını idempotent guard'a al (`başlatıldı` ref'i) | StrictMode dev'de effect'i iki kez koşturur → yerel QA ilk-tur deneyimini (ILK_SPAWN 4-6sn, 'İlk tur' mesajı, paylaşım-birincil CTA) HİÇ göremeden onaylıyor.
8. **content/sosyal-obezite-feed.ts:115-121** | Dinamik dalda `dk = turNo` (floor+1 değil — turNo mesajın öznesi, sayaç drift'ine kapalı); koşulu da `i < TUR_MESAJLARI.length` yap ki kilitli 6. mesaj kullanılsın | 6. tur '6. tur. Toplam 5 dakika' gösteriyor; spec §8'in kilitli kopyasındaki sayı asla görünmüyor, TUR_MESAJLARI[5] ölü sabit.
9. **submit/route.ts:37, :97, :103** | `skor` alanını optional yap + yalnız log'a al (şemadan tamamen ÇIKARMA — aklaYatkin imzası engine.ts:170 ortak dosya, istemci kullanımı kırılır); 422 gövdesinden `sebep`i çıkar, yalnız HILE_REDDI dön; sebep/threshold_name Sentry+PostHog'a | Spec §5.1 'payload'da skor HİÇ yok' + §6 'threshold yalnız event'e' — eşik adları saldırgana deneme-yanılma haritası veriyor.
10. **OyunKabuk.tsx:166-170** | Süre satırını text-base/lg beyaz ara kademeye çıkar; skor tek başına text-xs ghost | Spec §7/§8 zorunlu 3-kademe hiyerarşi 2'ye inmiş; 'en çok fark eden kazanır' savunma hattının taşıyıcısı.
11. **SosyalObezite.tsx:226 + :412-414** | `--kalan`ı tüketen görsel ekle: scaleX'li ince bar veya küçük SVG stroke-dashoffset (conic-gradient DEĞİL — her frame arka-plan repaint) | Yazım var, okuyan CSS sıfır: ölü per-frame invalidation + spec §2.2 'daralan süre çemberi' affordance'ı yok, %90 ilk-tur yakalama ölçütü desteksiz.
12. **lib/posthog.ts:54 + app/layout.tsx:165** | /sosyal-obezite'de autocapture'ı kapat (manuel capture — spec §4); CompassTint'in oyun rotasında render'ını değerlendir | Route-group çürütmesinden arta kalan iki gerçek kalem.
13. **SosyalObezite.tsx:194-195** | İpucu süresi 1600→4000ms + görünürken dokununca erken kapat; ve literal yerine `ILK_KACIRMA_IPUCU` import et (feed.ts:152 — tek-kaynak) | Oyunun tek öğretici mesajı 49 karakter/1.6 sn'de okunamıyor; kopya drift riski.
14. **OyunKabuk.tsx:205-252** | Store-ikincil dala onaylı C1 kopyası ('Bu oyunu kapat. Gerçek bir masaya otur.') + 'bir tur daha' yanına eşit ağırlıkta 'bırak' | 'Onurlu çıkış' anlatısı (kriz savunmasının kendisi) hiçbir yüzeyde yok; quit_click (§11) üretilemiyor. NOT: 'Bırakabildin' kart göstergesi AYRI onaya tabi — EKLEME.

## 3. SONRAYA BIRAKILABILIR

1. **submit/route.ts:38-44** | Zod tavanları: kart≤5000, sureMs≤62_000, yakalanan/kacan≤12, yanlisDokunma≤600 | Bugün istismar yok (skor=0'a çöker), gelecekteki formül değişikliğine sigorta.
2. **submit/route.ts:115-126** | Günlük run-yazım sayacı + PostHog alarmı (spec §6 zaten istiyor); TTL'siz karar KORUNUR | Tek IP/hafta ~50MB tavan; felaket değil, görünürlük eksiği.
3. **SosyalObezite.tsx:268, :381** | setPointerCapture (hover-scroll tuhaflığı + iOS pointercancel kenarları; releasePointerCapture GEREKSIZ — implicit) ve ayrı onCancel handler (`surukleniyor=false; hiz=0`, tap değerlendirmesine girmez) | Kozmetik hijyen; çürütülen sonsuz-kayma iddiasının kalıntısı.
4. **SosyalObezite.tsx:28-29** | HAVUZ 12→16 veya mount'ta `ceil(viewportH/132)+2` | >1452px viewport'ta alt boşluk + kenarda görünür recycle; telefonda sorun yok.
5. **SosyalObezite.tsx:195, :391** | setTimeout id'sini ref'te tut + cleanup'ta clearTimeout; kart ref'lerini stabil callback'e al; :20-21 'commit=0' yorumunu gerçeğe çevir | Sızıntı kalıbı + yanıltıcı sözleşme; fiilen bozulan şey yok.
6. **BeanCozulme.tsx:149-163** | 66 rect'i baştan mount et, yalnız opacity ile aç/kapa | Mount-anı final-opacity yüzünden :160 transition hiç çalışmıyor (ölü kod), bloklar snap ediyor; jank iddiası ölçümsüz.
7. **SosyalObezite.tsx:362, :369** | Oynanış HUD çiplerinde backdrop-blur → opak bg-elevated | Her frame 2 blur pass; etki ölçülmemiş, güvenli küçültme.
8. **BeanCozulme.tsx:125, :134** | `transition: 'd 900ms'` Safari'de snap — opacity crossfade'e çevir VEYA bilinçli-kabul notu düş | DUSUK; lansman blokajı değil.
9. **SosyalObezite.tsx:47-53** | Kademe→FeedKarti[] haritasını modül seviyesinde kur; rng'ye DOKUNMADAN (davranış-eşdeğer hash yazılmadan determinizm :46 bozulur) | Recycle başına filter+2 closure; modern GC için ucuz.

## 4. HENUZ YAZILMAMIS PARCALAR

1. **Paylaşım OG route'u** — YAZILMIŞ (bkz §0 çürütme). Kalan spec §7 farkları: `?ref=runId` challenge parametresi + hayalet-hedef gösterimi yok; runId UUID-regex'i page.tsx:35'te zaten var.
2. **Skor tablosu UI + okuma endpoint'i** — YOK. Zorunlu sıra: §1-madde-1 (anonId modeli) BİTMEDEN yazma — ZRANGE member=anonId → nick hash'ten ada çevir, anonId istemciye sızmaz. Spec §11: varsayılan sekme Bugün + mutlak sıra yerine yüzdelik (submit:136-139 bugün lb:all mutlak sıra dönüyor — o da bu işte düzelir). Kenar: ZCARD<20 iken yüzdelik saçma görünür ('bugün N oyuncudan M.sin' gösterimine düş).
3. **KVKK aydınlatma sayfası** — YOK. Takma ad + IP (ratelimit) + anonId cookie işleniyor; anonId cookie'si (§1-madde-1) zorunlu-çerez sınıfında metne girmeli.
4. **Playwright + axe kurulumu** — YOK. Spec §9 kabulü 'rıza-öncesi 0 localStorage (ph_*)' + §4 Profiler/long-task ölçümü + §3 %90 ilk-tur yakalama bunlarsız doğrulanamaz; §2 ve §3'teki a11y fix'lerinin regresyon bekçisi de bu.
5. **PostHog event şeması** — YOK. Asgari: run_submit (threshold_name burada — §2-madde-9 sebep taşınması buna bağlı), quit_click (§2-madde-14'e bağlı), run-yazım sayaç alarmı (§3-madde-2), paylaşım/CTA tıklamaları. /sosyal-obezite'de autocapture kapatılınca (§2-madde-12) manuel şema tek veri kaynağı olur.

## 5. CELISKILER (seçimler)

1. **GETDEL telafisi**: Güvenlik boyutu 'sess'i SET NX ile geri yaz' dedi; hasım 'restore kendi yarışını açar' dedi → **hasımı seçtim**: pipeline + 500/409 mesaj ayrımı asıl fix; restore best-effort opsiyonel.
2. **Tap eşiği**: A11y boyutu 24px önerdi; hasım '24px kısa kaydırmayı tap yapar, haksız -20' dedi → **500ms + 16-18px + yakala düğmesi** (hasım).
3. **Tur mesajı dk hesabı**: 'floor(toplamSaniye/60)+1' vs 'dk=turNo' → **dk=turNo** (iki hasım eleştirisi de aynı yönde: toplamSaniye ref'i yenilemede sıfırlanır, turNo drift'e kapalı).
4. **skor alanı**: Spec boyutu 'şemadan çıkar' dedi; hasım 'aklaYatkin ortak-dosya imzası kırılır' dedi → **optional + yalnız log** (hasım).
5. **ghost kontrastı**: 'token'ı global ≥#8A8A8A'ya çek' vs 'yalnız oyun yüzeylerinde sınıf değiştir' → **yerel sınıf** (hasım; site geneli regresyon taraması gerektirmez).
6. **Süre çemberi**: conic-gradient (denetim) vs scaleX-bar/SVG (hasım, her-frame repaint'ten kaçınır) → **hasım**.
7. **visibilitychange fix kapsamı**: Doğruluk boyutu istemci-only fix önerdi; hasım 'TTL hizalanmadan skor yine 409'a düşer' dedi → **birleşik fix** (§1-madde-3, istemci+sunucu birlikte).
8. **iskelet bilinmeyen-harf**: 'sil' (mevcut) vs 'reddet' (hasım — silme iskeleti kısaltıp taklidi açık bırakır) → **reddet**.
9. **Route-group**: Perf boyutu KRITIK 'sayfayı (game) grubuna taşı' dedi; hasım SiteKatmanlari'nın eşdeğer-etkiyi ~30 satırda sağladığını ve taşımanın bilinçli-reddedilmiş yüksek-risk refactor olduğunu gösterdi → **taşıma YOK**, yalnız PostHog+CompassTint kalemleri (§2-madde-12).
10. **commit=0**: 'HUD'u tam rAF'a al' (harfiyen) vs 'kabul kriterini revize et' → **iki aşama**: önce shallow-compare+izolasyon (ucuz, ~%80), hedef rAF-HUD; kriter revizyonu ancak kullanıcı onayıyla (spec kabul testi aksi halde FAIL).

## 6. KARARLARA AYKIRI ONERILER (ayrı onay gerektirir — UYGULANMADI)

1. **'Sakin tur' 120sn modu** (Erişilebilirlik#9): 60 sn tur süresi oyunun tezi ve karar seti içinde; ayrıca TUR_SURESI_MS sabit import + spawnTakvimi 60sn'ye kilitli — 'tek satır' değil motor değişikliği. Kullanıcı kararı olmadan ekleme.
2. **run:{runId} TTL'siz kararı** (spec §0, bilinçli): Güvenlik boyutunun 'TTL ekle / paylaşım anında yaz' alternatifleri karara aykırı — yalnız sayaç+alarm (§3-madde-2) uygulanır.
3. **'Bırakabildin' kart göstergesi** (spec §13/3): ayrı onaya tabi — §2-madde-14 yalnız düz 'bırak' seçeneğini ekler, göstergeyi EKLEMEZ.
4. **`--kalan` satırını silme alternatifi** (Performans#4): spec §2.2 affordance'ını gömer — yalnız bilinçli spec-sapma kaydıyla kabul edilebilir; seçilen yol bağlamak (§2-madde-11).
5. **Spec-içi gerginlik (karar sahibine sorulmalı)**: §5.1 'tavan-üstü submit'i sessizce kabul et, bayrak verme' ile §5.2 'reddedilen oturumda HILE_REDDI göster' çelişiyor — §2-madde-9 sebep-sızıntısını kapatır ama sessiz-kabul/görünür-red seçimi karar gerektirir.
6. **Sert replay koruması önerilmedi** (karar 10 uyarınca): seed-istemcide/bot-mükemmel-tur bilinen kabul edilmiş risk, hiçbir madde bunu yeniden açmaz.